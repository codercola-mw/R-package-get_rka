library(shiny)

# Define server logic required to use the function
ui <- shinyUI(pageWithSidebar(
  
  # Application title
  headerPanel("Showing kolada data / Lab 5"),
  
  sidebarPanel(),
  
  mainPanel()
))

shinyServer(function(input, output) {
  
})